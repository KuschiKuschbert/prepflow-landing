// Memberstack configuration for v2
export const memberstackConfig = {
  publicKey: process.env.NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY || '',
  // Add your Memberstack public key here or set it as an environment variable
};
