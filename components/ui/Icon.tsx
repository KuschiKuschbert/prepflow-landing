'use client';

/**
 * Re-export Icon from lib for backward compatibility.
 * Components and app import from here; lib imports from @/lib/ui/Icon.
 */
export { Icon, type IconProps, type IconSize } from '@/lib/ui/Icon';
