'use client';

// Replace COGS Calculator with Dish Builder
// Dish Builder provides all COGS calculation functionality with a modern tap-to-add interface
import DishBuilderClient from '../../dish-builder/components/DishBuilderClient';

export function CalculatorTab() {
  return <DishBuilderClient />;
}
