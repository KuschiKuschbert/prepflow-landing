/**
 * CSS styles for combined menu export (menu display + allergen matrix)
 */

import { menuDisplayStyles } from './menuDisplayStyles';
import { allergenMatrixStyles } from './allergenMatrixStyles';

export const COMBINED_EXPORT_STYLES = `${menuDisplayStyles}${allergenMatrixStyles}`;
