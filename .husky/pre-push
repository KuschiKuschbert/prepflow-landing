#!/bin/sh
# Lightweight pre-push: format + lint + type-check (< 30s)
# Full verification runs in CI. Use safe-merge for local merge workflow.
if [ "$CI" = "true" ]; then
  exit 0
fi
npm run format:check && npm run lint && npm run type-check
