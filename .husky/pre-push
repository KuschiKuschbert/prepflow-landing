#!/bin/sh
# Lightweight pre-push: lint + type-check only (< 30s)
# Full verification runs in CI. Use safe-merge for local merge workflow.
if [ "$CI" = "true" ]; then
  exit 0
fi
npm run lint && npm run type-check
